{
  "mcpServers": {
    "traderisk-skills": {
      "command": "node",
      "args": ["./dist/minimal-server.js"],
      "env": {
        "SUPABASE_URL": "${SUPABASE_URL}",
        "SUPABASE_SERVICE_ROLE_KEY": "TODO: segredo - usar variável de ambiente, nunca commitar"
      }
    }
  }
}
